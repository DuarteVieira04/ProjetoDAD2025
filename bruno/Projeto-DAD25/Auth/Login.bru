meta {
  name: Login
  type: http
  seq: 1
}

post {
  url: {{api_url}}/auth/login
  body: json
  auth: none
}

body:json {
  {
    "email":"john@example.com",
    "password":"123456"
  }
}

tests {
  let data = res.getBody();
  bru.setEnvVar("token",data.token);
}
